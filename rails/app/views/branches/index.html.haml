%h1 Listing branches

%table
  %thead
    %tr
      %th Address
      %th
      %th
      %th

  %tbody
    - @branches.each do |branch|
      %tr
        %td= branch.address
        %td= link_to 'Show', branch
        %td= link_to 'Edit', edit_branch_path(branch)
        %td= link_to 'Destroy', branch, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Branch', new_branch_path
